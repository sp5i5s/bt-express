let config = {
    // 数据库配置表
    database : {
        host : '127.0.0.1',
        database : 'ysy_cloud',
        user : 'root',
        password : ''
    },
    // Controller目录层级
    controller_level : 1
}

module.exports = config;
